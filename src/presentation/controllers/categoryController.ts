import { Request,Response, NextFunction } from "express";

export class CategoryController{
    constructor(
        
    ) { }
    
    fetchAllCategory = async(req:Request,res:Response,next:NextFunction)
}